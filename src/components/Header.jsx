import React from 'react';
import Navbar from './NavBar';
import '../styles/Header.scss';

const Header = () => {
    return (
        <header>
            <Navbar />
        </header>
    );
};

export default Header;