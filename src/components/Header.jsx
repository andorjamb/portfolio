import React from 'react';
import Nav from './NavBar';
import '../styles/Header.scss';

const Header = () => {
    return (
        <header>
            <Nav />
        </header>
    );
};

export default Header;